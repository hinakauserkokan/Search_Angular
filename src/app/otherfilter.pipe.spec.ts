import { OtherfilterPipe } from './otherfilter.pipe';

describe('OtherfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OtherfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
